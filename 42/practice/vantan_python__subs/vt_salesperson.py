from vt_product import Product

class Salesperson:
	def __init__(self, name='nameless salesperson.'):
		self.name = name
	def __str__(self):
		return (f"{self.name}")
	def promote(self):
		productA = Product()
		return (f"{productA}")
